var users = [];

// Função para adicionar um novo usuário
function addUser(user) {
    users.push(user);
}

// Exemplo de uso:
var novoUsuario = {
    username: "novousuario",
    password: "novasenha"
};